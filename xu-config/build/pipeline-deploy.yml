trigger:
  branches:
    include:
    - dev/*
    - main
    - qas
    - prod
  paths:
    include: 
    - xu-config/*

extends:
  template: ../../build/stages-build-deploy.yml
  parameters:
    folder: xu-config
    solution: XtractUniversal
    deploy: true
