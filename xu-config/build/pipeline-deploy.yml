trigger:
  branches:
    include:
    - dev_mpors
    - dev_fbouw
    - dev_tdorodnova
    - dev_nkukhtenko
    - dev_sleshchuk
    - main
    - qas
    - prod
  paths:
    include: 
    - xu-config/*

extends:
  template: ../../build/stages-build-deploy.yml
  parameters:
    folder: xu-config
    solution: XtractUniversal
    deploy: true
    # cleanWorkspace: true # https://learn.microsoft.com/en-us/azure/devops/pipelines/process/phases?view=azure-devops&tabs=yaml#workspace
    # somehow just clean the artifact
