trigger: none
  # branches:
  #   include:
  #   - dev_mpors
  #   - dev_fbouw
  #   - dev_tdorodnova
  #   - dev_nkukhtenko
  #   - dev_sleshchuk
  #   - dev/*
  #   - main
  #   - qas
  #   - prod
  # paths:
  #   include: 
  #   - xu-config/*

extends:
  template: ../../build/stages-build-deploy.yml
  parameters:
    folder: xu-config
    solution: XtractUniversal
    deploy: true
    clean: all # TODO or should this be done before BUILD job?
