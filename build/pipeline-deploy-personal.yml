trigger:
- none

extends:
  template: pipeline-deploy.yml
  variables:
    dataFactoryName: "df-$(user)-d-euw-001"
    sqlDatabaseName: "sqldb-$(user)-d-euw-001"
    synapseSqlPool: "syndw_$(user)_d_euw_001"
    storageAccountName: "st$(user)deuw001"
    xuPrefix: "$(user)"
