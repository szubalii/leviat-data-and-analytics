# .NET Desktop
# Build and run tests for .NET Desktop or Windows classic desktop solutions.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/apps/windows/dot-net

trigger:
- none

variables:
- group: DnA_DEV
- group: KeyVault_DEV

jobs:

  # Start VM
- template: ./jobs-start-vm

# Build jobs
- template: ../orchestration/build/jobs-build.yml
- template: ../orchestration/build/jobs-artifact.yml
- template: ../synapse-dwh/build/jobs-build.yml
- template: ../synapse-dwh/build/jobs-artifact.yml
- template: ../azure-data-factory/build/jobs-build.yml
- template: ../azure-data-factory/build/jobs-artifact.yml