trigger:
- none

variables:
- group: DnA_DEV
- group: KeyVault_DEV
# - name: sqlDatabaseName
#   value: "sqldb-xxxx-sls-d-euw-001"
# - name: synapseSqlPool
#   value: "syndw_xxxx_sls_d_euw_001"

jobs:
- template: ./jobs-build.yml
  parameters:
    artifact: true
# - template: ./jobs-pre-build.yml
# # ADF: only validate
# - template: ../azure-data-factory/build/jobs-build.yml
# # OrchDB: only validate, build, deploy, and test
# - template: ../orchestration/build/jobs-build.yml
#   parameters:
#     deploy: true
#     test: true
# # Synapse: only validate, build, deploy, and test
# - template: ../synapse-dwh/build/jobs-build.yml
#   parameters:
#     deploy: true
#     test: true
# -template: ./jobs-test.yml