# Azure Synapse Dedicated SQL Pool Deploy Job
jobs:
- job: SynapseDWHDeploy

  # dependsOn: ${{if(eq($(deployEnv),'personal'),SynapseDWHBuild,)}}
  pool:
    name: 'VM'
    demands:
    - node.js
    - VisualStudio
    - Python.Version

  steps:
  - template: ./steps-deploy.yml