trigger: none
  # branches:
  #   include:
  #   - dev_mpors

# resources:
#   pipelines:
#   - pipeline: validate
#     source: Validate
#     trigger:
#       branches:
#         include:
#         - dev_mpors
      

variables:
- group: DnA_DEV
# - group: KeyVault_DEV

jobs:
- template: ../../build/jobs-pre-build.yml
- template: ./jobs-build.yml