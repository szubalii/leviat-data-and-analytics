# Orchestration Database Build & Deploy Job
jobs:
- job: OrchDBBuild&Deploy
  dependsOn: StartVM
  pool:
    name: 'VM'
    demands:
    - node.js
    - VisualStudio
  variables:
    orchDBSolution: '**/orchestration/*.sln'
    buildPlatform: 'Any CPU'
    buildConfiguration: 'Release'
    orchDBContainerURL: https://$(storageAccountName).blob.core.windows.net/orch-db

  steps:

  - template: ./steps-build.yml
