trigger:
  branches:
    include: 
    - dev/*
    - main
    - qas
    - prod
  paths:
    include: 
    - orchestration/*

extends:
  template: ../../build/stages-build-deploy.yml
  parameters:
    folder: orchestration
    solution: Orchestration
    hasVariables: true
    deploy: true