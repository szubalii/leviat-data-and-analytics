trigger: none # triggered by PR

jobs:
  job: TEST_ORCH_DB
    variables:
    - name: buildConfiguration
      value: 'Debug'
    pool:
      vmImage: 'ubuntu-20.04'
    steps:
    - template: ./steps-test.yml