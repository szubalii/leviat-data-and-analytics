trigger:
  branches:
    include: 
    - dev/*
    - main
    - qas
    - prod
  paths:
    include: 
    - azure-data-factory/*

extends:
  template: ../../build/stages-build-deploy.yml
  parameters:
    folder: azure-data-factory
    solution: ADF
    deploy: true
  