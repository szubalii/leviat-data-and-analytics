# Azure Data Factory Deploy Job
jobs:
- job: ADFDeploy
  # dependsOn: ${{if(eq($(deployEnv),'personal'),ADFBuild,)}}
  pool:
    name: 'VM'
    demands:
    - node.js
    - VisualStudio

  steps:

  - template: ./steps-deploy.yml